# Baked particle forms. Generated by `pnpm bake` from the product meshes in the
# sibling primal repo, which CI does not have, so they are committed. Binary:
# git must never try to diff or merge them.
public/forms/*.bin binary
